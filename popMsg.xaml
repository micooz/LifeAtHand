<UserControl x:Class="Life_at_hand.popMsg"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30.996" d:DesignWidth="192.62" Height="30" Width="190">
    <Grid x:Name="GridMain">
        <Border BorderThickness="1" Cursor="Hand" CornerRadius="2" IsHitTestVisible="False">
            <Border.Background>
                <SolidColorBrush Color="Black" Opacity="0.5"/>
            </Border.Background>
            <Label x:Name="txtMsg" Content="set popmsg here" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Label.Effect>
                    <DropShadowEffect ShadowDepth="0" Direction="300"/>
                </Label.Effect>
            </Label>
        </Border>

    </Grid>
</UserControl>
